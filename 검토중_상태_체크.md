# "검토 중" 상태 체크 분석

## 결론 요약

> **검토 중(reviewing) 상태는 절반만 구현된 상태입니다.**  
> 숫자는 항상 0으로 표시되며, 실제로 이 상태로 전환할 방법이 없습니다.

---

## 구현 현황

| 항목 | 파일 | 상태 |
|---|---|---|
| DB 필드 (`status String`) | `prisma/schema.prisma` | ✅ 있음 |
| 상태 레이블/색상 정의 | `src/lib/utils.ts:110` | ✅ 있음 |
| 검수 대기 목록 쿼리 포함 | `src/app/admin/page.tsx:31` | ✅ 있음 |
| stats 카운트 표시 | `src/app/admin/page.tsx:50,83` | ✅ 있음 (항상 0) |
| `reviewing` 상태 전환 **UI 버튼** | `AdminWorkerActions.tsx` | ❌ **없음** |
| `reviewing` 상태 전환 **API** | `ADMIN_TARGET_STATUSES` | ❌ **없음** |

---

## 문제 상세

### 1. `ADMIN_TARGET_STATUSES`에 `reviewing` 미포함

```ts
// src/lib/admin-worker-status.ts
export const ADMIN_TARGET_STATUSES = ["approved", "published", "rejected"] as const;
```

`reviewing`이 목표 상태 목록에 없어, API를 통해 이 상태로 변경하는 것 자체가 불가능합니다.

---

### 2. `AdminWorkerActions`에 "검토 중" 버튼 없음

```tsx
// src/components/worker/AdminWorkerActions.tsx
{currentStatus === "submitted" && (
  <button onClick={() => handleAction("approve")} ...>승인</button>
)}
// "검토 중으로 변경" 버튼 없음
```

현재 관리자가 할 수 있는 액션:
- `submitted` → 승인 / 반려
- `approved` → 게시 / 반려
- 그 외 → 반려만 가능

`submitted → reviewing` 전환 경로가 없으므로 해당 상태는 절대 도달 불가능합니다.

---

### 3. stats 카드 "검토 중"은 항상 0

```ts
// src/app/admin/page.tsx
db.aIWorker.count({ where: { status: "reviewing" } })
```

DB에 `reviewing` 상태인 워커가 생길 수 없으므로 이 숫자는 항상 0입니다.

---

## 해결 방안

### 방안 A — `reviewing` 상태 완성 구현 (권장)

1. `ADMIN_TARGET_STATUSES`에 `"reviewing"` 추가
2. `canTransitionWorkerStatus`에 `submitted → reviewing` 전환 허용 추가
3. `AdminWorkerActions`에 "검토 중으로 변경" 버튼 추가 (`currentStatus === "submitted"` 일 때)
4. stats 카드에 `#review-queue` 앵커 연결 (이미 됨)

### 방안 B — `reviewing` 상태 제거

검토 중 상태가 실제로 필요하지 않다면:
1. stats 카드에서 "검토 중" 항목 제거
2. `WORKER_STATUSES`에서 `reviewing` 레이블 제거
3. 검수 대기 쿼리에서 `"reviewing"` 조건 제거

---

## 관련 파일

| 파일 | 역할 |
|---|---|
| `src/lib/admin-worker-status.ts` | 상태 전환 규칙 정의 |
| `src/components/worker/AdminWorkerActions.tsx` | 관리자 액션 버튼 UI |
| `src/app/api/admin/workers/[id]/review/route.ts` | 상태 변경 API |
| `src/app/admin/page.tsx` | 통계 카드 및 검수 목록 |
| `src/lib/utils.ts` | 상태 레이블/색상 정의 |
