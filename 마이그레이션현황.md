# SQLite → PostgreSQL 마이그레이션 현황

**날짜:** 2026-06-02  
**프로젝트:** ai-worker-platform  
**DB:** SQLite → Supabase PostgreSQL (ap-northeast-2, 서울)

---

## 완료 항목

- [x] SQLite 패키지 제거: `@prisma/adapter-better-sqlite3`, `better-sqlite3`, `@types/better-sqlite3`
- [x] PostgreSQL 어댑터 설치: `@prisma/adapter-pg`, `pg`, `@types/pg`
- [x] `prisma/schema.prisma`: provider `sqlite` → `postgresql`
- [x] `prisma.config.ts`: `directUrl` 추가
- [x] `src/lib/db.ts`: `PrismaBetterSqlite3` → `PrismaPg` 교체
- [x] `.env`: Supabase DATABASE_URL / DIRECT_URL 설정
- [x] `.env.example`: Supabase 형식으로 업데이트
- [x] `prisma/migrations/` SQLite 히스토리 삭제

---

## 진행 중

- [ ] `prisma migrate dev --name init` 실행 중
  - Supabase DB 연결 확인됨: `aws-1-ap-northeast-2.pooler.supabase.com:6543`
  - 마이그레이션 완료 대기 중

---

## 남은 작업

- [ ] 마이그레이션 완료 확인: `npx prisma migrate status`
- [ ] Seed 데이터 삽입: `npx prisma db seed`
- [ ] 앱 로컬 실행 테스트: `npm run dev`
- [ ] SQLite db 파일 삭제 (`prisma/dev.db`, `prisma/prisma/dev.db`, `dev.db`)
- [ ] Vercel 환경변수 설정 (DATABASE_URL, DIRECT_URL)

---

## Supabase 연결 정보

| 항목 | 값 |
|------|-----|
| 프로젝트 ref | `tudqkmfbiipsiqgeitfb` |
| 리전 | ap-northeast-2 (서울) |
| DATABASE_URL 포트 | 6543 (Transaction pooler) |
| DIRECT_URL 포트 | 5432 (Direct) |
