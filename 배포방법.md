# 배포 방법

## 1. 현재 프로젝트 기준 권장 배포 방식

이 프로젝트는 다음 구조를 사용합니다.

- Next.js 16 App Router
- Prisma + SQLite + better-sqlite3
- NextAuth
- Stripe 결제/웹훅
- OpenAI API

현재 구조에서는 **영구 디스크를 붙일 수 있는 Linux 서버 또는 VPS 배포**가 가장 안전합니다.

이유:

- DB가 SQLite 파일 기반입니다.
- `DATABASE_URL`이 로컬 파일 경로를 가리키도록 설계되어 있습니다.
- 서버리스 환경(Vercel 기본 구성)에서는 파일 DB가 재배포/스케일링 시 안전하게 유지되지 않습니다.

권장 순서:

1. 1차 운영은 Linux VPS에 배포
2. 운영이 안정되면 SQLite를 Postgres 또는 Turso로 이전
3. 그 후 Vercel 같은 서버리스 환경으로 옮기는 것을 검토

## 2. 배포 전에 먼저 확인할 것

로컬에서 아래 명령이 정상 동작해야 합니다.

```bash
npm install
npx prisma generate
npm run lint
npm run build
```

추가 확인 사항:

- Stripe 테스트 키로 결제 흐름 확인
- Stripe 웹훅 수신 확인
- 로그인/회원가입 확인
- AI 실행 API 확인
- 관리자 승인 후 게시 흐름 확인

## 3. 필수 환경변수

배포 서버의 `.env` 또는 호스팅 환경변수에 아래 값을 넣어야 합니다.

```env
DATABASE_URL="file:/var/www/ai-worker-platform/data/prod.db"

NEXTAUTH_SECRET="32바이트이상랜덤문자열"
NEXTAUTH_URL="https://your-domain.com"

NEXT_PUBLIC_APP_URL="https://your-domain.com"

OPENAI_API_KEY="sk-..."

STRIPE_SECRET_KEY="sk_live_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

PLATFORM_FEE_PERCENT=20
```

주의:

- 프론트엔드 결제 화면은 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`를 사용합니다.
- 현재 `.env.example`과 `vercel.json`에는 `STRIPE_PUBLISHABLE_KEY`가 들어 있는데, 실제 클라이언트 코드는 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`를 읽습니다.
- 배포 시에는 반드시 **`NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`** 를 설정해야 합니다.

## 4. Linux 서버 배포 절차

아래 예시는 Ubuntu + Node.js + PM2 + Nginx 기준입니다.

### 4.1 서버 준비

```bash
sudo apt update
sudo apt install -y nginx
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g pm2
```

Node 버전 확인:

```bash
node -v
npm -v
```

### 4.2 프로젝트 업로드

```bash
cd /var/www
sudo mkdir -p ai-worker-platform
sudo chown $USER:$USER ai-worker-platform
cd ai-worker-platform
```

Git으로 가져오는 경우:

```bash
git clone <저장소주소> .
```

### 4.3 SQLite 저장 디렉터리 생성

```bash
mkdir -p /var/www/ai-worker-platform/data
```

`DATABASE_URL` 예시:

```env
DATABASE_URL="file:/var/www/ai-worker-platform/data/prod.db"
```

중요:

- Next.js 프로세스가 이 경로에 쓰기 가능해야 합니다.
- 운영 DB 파일은 프로젝트 루트의 `dev.db`를 그대로 쓰지 않는 것이 좋습니다.

### 4.4 환경변수 파일 생성

서버에 `.env` 파일을 작성합니다.

```bash
nano .env
```

위 3번의 환경변수를 실제 값으로 채워 넣습니다.

### 4.5 의존성 설치 및 Prisma 준비

```bash
npm ci
npx prisma generate
npx prisma migrate deploy
```

참고:

- 새 운영 DB라면 위 명령으로 스키마가 생성됩니다.
- 현재 저장소에는 마이그레이션 파일이 있으므로 `prisma migrate deploy` 기준으로 운영하는 것이 안전합니다.

### 4.6 빌드 및 실행

```bash
npm run build
pm2 start npm --name ai-worker-platform -- start
pm2 save
pm2 startup
```

상태 확인:

```bash
pm2 status
pm2 logs ai-worker-platform
```

기본적으로 앱은 `3000` 포트에서 실행됩니다.

## 5. Nginx 리버스 프록시 설정

예시 설정 파일:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

적용:

```bash
sudo nano /etc/nginx/sites-available/ai-worker-platform
sudo ln -s /etc/nginx/sites-available/ai-worker-platform /etc/nginx/sites-enabled/ai-worker-platform
sudo nginx -t
sudo systemctl reload nginx
```

## 6. HTTPS 적용

Let's Encrypt 사용 예시:

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

HTTPS 적용 후 아래 값을 반드시 실제 도메인으로 맞춥니다.

- `NEXTAUTH_URL`
- `NEXT_PUBLIC_APP_URL`
- Stripe Dashboard의 webhook URL

## 7. Stripe 설정

운영 결제를 쓰려면 Stripe Dashboard에서 아래를 맞춰야 합니다.

- 공개키: `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- 비밀키: `STRIPE_SECRET_KEY`
- 웹훅 시크릿: `STRIPE_WEBHOOK_SECRET`

Webhook endpoint 예시:

```text
https://your-domain.com/api/payments/webhook
```

권장 이벤트:

- `payment_intent.succeeded`

배포 후 확인:

- 결제 성공 후 `Purchase.paymentStatus`가 `completed`로 바뀌는지 확인
- `AIWorker.totalSales`가 증가하는지 확인
- 메이커 수익(`MakerProfile.totalEarned`)이 반영되는지 확인

## 8. Vercel 배포는 왜 바로 권장하지 않는가

현재 프로젝트에는 `vercel.json`이 있지만, 아래 이유로 바로 운영 배포에 쓰기 어렵습니다.

- SQLite 파일 DB는 Vercel 서버리스 환경과 궁합이 좋지 않습니다.
- 인스턴스가 고정되지 않으므로 파일 기반 DB 지속성이 깨질 수 있습니다.
- Stripe webhook 처리도 파일 DB 상태와 함께 운영 안정성이 떨어질 수 있습니다.

추가로 확인된 점:

- `vercel.json`은 `STRIPE_PUBLISHABLE_KEY`를 사용하지만,
- 실제 클라이언트 코드는 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`를 사용합니다.

즉, 현재 상태로는 Vercel 환경변수 이름도 바로 맞지 않습니다.

## 9. Vercel로 꼭 배포해야 한다면

아래 선행 작업이 필요합니다.

1. DB를 SQLite에서 Postgres 또는 Turso로 이전
2. Prisma datasource/provider 정리
3. 배포 환경변수 이름 정리
4. Stripe webhook 운영 URL 재설정
5. 재배포/스케일링 시 데이터 일관성 검증

이 작업이 끝나면 Vercel 배포가 현실적입니다.

## 10. 운영 점검 체크리스트

- 도메인 접속 정상
- 로그인/회원가입 정상
- 관리자 계정 로그인 정상
- 메이커가 AI 직원 생성 가능
- 관리자 승인/게시 가능
- 무료 AI 직원 실행 가능
- 유료 결제 가능
- Stripe webhook 수신 정상
- 구매 후 대시보드 반영 정상
- OpenAI 호출 정상
- 서버 재시작 후 SQLite 데이터 유지 확인

## 11. 장애 시 우선 확인 항목

### 앱이 안 뜰 때

```bash
pm2 logs ai-worker-platform
```

### Nginx 문제일 때

```bash
sudo nginx -t
sudo journalctl -u nginx -n 100
```

### DB 경로 문제일 때

확인할 것:

- `DATABASE_URL` 경로가 절대경로인지
- 해당 경로에 쓰기 권한이 있는지
- DB 파일이 실제 생성되었는지

### Stripe 결제가 안 될 때

확인할 것:

- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`
- Stripe webhook 대상 URL
- 배포 도메인과 `NEXT_PUBLIC_APP_URL` 일치 여부

## 12. 한 줄 결론

현재 구조 그대로라면 **Vercel보다 영구 디스크가 있는 Linux 서버 배포가 맞고**, 서버 배포 후 안정화한 다음 DB를 외부형으로 바꾸고 서버리스를 검토하는 순서가 가장 안전합니다.