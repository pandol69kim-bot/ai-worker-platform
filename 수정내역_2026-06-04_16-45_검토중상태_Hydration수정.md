# 수정내역 — 2026-06-04 16:45
**작업 키워드**: 검토중 상태 활성화 / Hydration 오류 수정

---

## 1. "검토 중" 상태 완성 구현

### 문제
`reviewing` 상태가 정의만 되어 있고 전환 UI/API가 없어 항상 0 표시

### 수정 파일 및 내용

#### `src/lib/admin-worker-status.ts`
- `ADMIN_TARGET_STATUSES`에 `"reviewing"` 추가
- `submitted → reviewing` 전환 규칙 추가
- 기존 `submitted/reviewing` 합산 조건 → `submitted`, `reviewing` 개별 분기로 분리

```ts
// 변경 전
if (["submitted", "reviewing"].includes(currentStatus)) {
  if (["approved", "rejected"].includes(targetStatus)) { ... }
}

// 변경 후
if (currentStatus === "submitted") {
  if (["reviewing", "approved", "rejected"].includes(targetStatus)) { ... }
}
if (currentStatus === "reviewing") {
  if (["approved", "rejected"].includes(targetStatus)) { ... }
}
```

#### `src/app/api/admin/workers/[id]/review/route.ts`
- `action` enum에 `"review"` 추가
- `STATUS_MAP` 객체로 action → status 변환 정리

```ts
const STATUS_MAP = {
  review:  "reviewing",
  approve: "approved",
  reject:  "rejected",
  publish: "published",
} as const;
```

#### `src/components/worker/AdminWorkerActions.tsx`
- `submitted` 상태일 때 **"검토 중"** 버튼(하늘색) 추가
- `reviewing` 상태일 때 **"승인"** 버튼 표시 (기존 submitted 전용 버튼 분리)
- `Clock` 아이콘 추가

#### `src/app/admin/page.tsx`
- Stats "검토 중" 카드 `<div>` → `<Link href="#review-queue">` 변경
- 하늘색(`sky`) 테마 적용

### 최종 상태 전환 흐름
```
submitted → [검토 중 버튼] → reviewing → [승인] → approved → [게시] → published
         → [승인 버튼]   → approved
         → [반려 버튼]   → rejected
reviewing → [반려 버튼]  → rejected
```

---

## 2. Hydration 오류 수정 — ThemeToggle

### 문제
```
+ disabled={true}   ← 서버 렌더
- disabled={null}   ← 클라이언트 hydration
```
`useSyncExternalStore`의 클라이언트 스냅샷 `() => true`가 hydration 시점에 즉시 실행되어 서버 HTML과 불일치 발생

### 수정 파일
**`src/components/layout/ThemeToggle.tsx`**

```ts
// 변경 전 — useSyncExternalStore 방식 (hydration 불일치)
const mounted = useSyncExternalStore(subscribe, () => true, () => false);

// 변경 후 — useEffect + useState 방식 (안전)
const [mounted, setMounted] = useState(false);
useEffect(() => {
  const stored = localStorage.getItem(STORAGE_KEY) === "dark" ? "dark" : "light";
  setTheme(stored);
  applyTheme(stored);
  setMounted(true);
}, []);
```

### 동작 원리
| 단계 | mounted | 렌더 결과 |
|---|---|---|
| 서버 SSR | `false` | `disabled` 버튼 |
| 클라이언트 hydration | `false` | `disabled` 버튼 (서버와 일치 ✅) |
| useEffect 실행 후 | `true` | 실제 테마 버튼 (정상 re-render) |

---

## 수정된 파일 목록
```
src/lib/admin-worker-status.ts
src/app/api/admin/workers/[id]/review/route.ts
src/components/worker/AdminWorkerActions.tsx
src/app/admin/page.tsx
src/components/layout/ThemeToggle.tsx
```
