# 수정내역 — 2026-06-04 17:05
**작업 키워드**: 반응형 UI (360px 모바일 대응)

---

## 1. 관리자 패널 회원 관리 — 360px 반응형

**파일**: `src/components/worker/AdminUserManager.tsx`

### 검색 + 역할 필터 바
- `flex` → `flex flex-col sm:flex-row` : 모바일에서 세로 스택
- 역할 필터 select: `w-36` → `w-full sm:w-36`

### 회원 목록
- **< 640px**: 테이블 숨김, 카드 뷰 표시
  - 아바타 + 이름(truncate) + 이메일(truncate) + 역할 배지 한 줄
  - 역할 변경 select 풀폭 + AI직원수·구매수 인라인 표시
- **640px+**: 기존 테이블 유지 (`hidden sm:block`)
- 이메일 `truncate max-w-45` 처리

### AI 직원 목록 (역할 필터 선택 시)
- 섹션 헤더: `flex-col sm:flex-row` 세로 스택
- 상태 필터 select: `w-full sm:w-32`
- **< 640px**: 카드 뷰 — 제목 + 상태 배지, 카테고리·AI 제공자, 메이커명(truncate)
- **640px+**: 기존 테이블 유지

---

## 2. market/[id] 상세 페이지 — 제목 영역 360px 수정

**파일**: `src/app/market/[id]/page.tsx`

### 문제
배지·별점·도입수가 `flex items-center gap-3` 한 줄로 고정 → 360px에서 오버플로

### 수정

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 배지 줄 레이아웃 | `flex items-center gap-3` | `flex flex-wrap gap-x-2 gap-y-1.5` |
| 제목 폰트 | `text-2xl` 고정 | `text-xl` / `sm:text-2xl` |
| 썸네일 크기 | `h-16 w-16` | `h-14 w-14` / `sm:h-16 sm:w-16` |
| 아이콘 크기 | `h-4 w-4` | `h-3.5 w-3.5` / `sm:h-4 sm:w-4` |
| 제목 컨테이너 | 폭 제한 없음 | `min-w-0 flex-1` |

---

## 수정된 파일 목록
```
src/components/worker/AdminUserManager.tsx
src/app/market/[id]/page.tsx
```
