# SQLite → PostgreSQL 마이그레이션 작업계획

**작성일:** 2026-06-02  
**프로젝트:** ai-worker-platform  
**브랜치:** master

---

## 현재 상태 분석

### SQLite 구성 요소
| 파일 | 내용 |
|------|------|
| `prisma/schema.prisma` | `provider = "sqlite"` (url 없음) |
| `prisma.config.ts` | `datasource.url = process.env.DATABASE_URL` |
| `src/lib/db.ts` | `PrismaBetterSqlite3` 어댑터 사용 |
| `.env` | `DATABASE_URL="file:./dev.db"` |
| `package.json` | `@prisma/adapter-better-sqlite3`, `better-sqlite3` 의존성 |

### DB 파일 위치 (제거 대상)
- `prisma/dev.db`
- `prisma/prisma/dev.db`
- `dev.db`

---

## 마이그레이션 작업 단계

### Phase 1: 패키지 교체

**제거할 패키지**
```bash
npm uninstall @prisma/adapter-better-sqlite3 better-sqlite3 @types/better-sqlite3
```

**설치 불필요** — PostgreSQL은 Prisma 표준 클라이언트로 어댑터 없이 동작

---

### Phase 2: Prisma 스키마 변경

**파일:** `prisma/schema.prisma`

```prisma
# 변경 전
datasource db {
  provider = "sqlite"
}

# 변경 후
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

**JSON 타입 선택적 개선** (현재 String으로 저장된 JSON 필드)
- `AIWorker.workflow` → `Json` 타입 고려
- `AIWorker.rules` → `Json` 타입 고려  
- `AIWorker.tags` → `String[]` 배열 타입 고려
- **판단:** 우선 기존 String 유지 → 스키마 안정화 후 별도 개선

---

### Phase 3: db.ts 클라이언트 교체

**파일:** `src/lib/db.ts`

```typescript
# 변경 전 (SQLite 어댑터)
import { PrismaBetterSqlite3 } from "@prisma/adapter-better-sqlite3";
import path from "path";

function createPrismaClient() {
  const dbUrl = process.env.DATABASE_URL?.replace("file:", "") ?? "./dev.db";
  const absolutePath = path.isAbsolute(dbUrl) ? dbUrl : path.join(process.cwd(), dbUrl.replace("./", ""));
  const adapter = new PrismaBetterSqlite3({ url: absolutePath });
  return new PrismaClient({ adapter, log: [...] });
}

# 변경 후 (표준 Prisma)
function createPrismaClient() {
  return new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["error", "warn"] : ["error"],
  });
}
```

---

### Phase 4: 환경변수 업데이트

**파일:** `.env`

```bash
# 변경 전
DATABASE_URL="file:./dev.db"

# 변경 후 (로컬 개발용 예시)
DATABASE_URL="postgresql://postgres:password@localhost:5432/ai_worker_platform"

# 변경 후 (Supabase 사용 시)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres"
```

> **Supabase/Vercel 서버리스 환경:** Connection Pooling을 위해 `DIRECT_URL`을 schema.prisma에 추가해야 함

**파일:** `.env.example`
```bash
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
# Supabase 사용 시 아래 추가
# DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
```

---

### Phase 5: prisma.config.ts 확인

현재 구성이 `process.env["DATABASE_URL"]`을 참조하므로 **변경 불필요**.  
단, Supabase처럼 `directUrl`이 필요한 경우:

```typescript
datasource: {
  url: process.env["DATABASE_URL"],
  directUrl: process.env["DIRECT_URL"],
},
```

---

### Phase 6: 마이그레이션 실행

```bash
# 기존 SQLite 마이그레이션 히스토리 초기화
rm -rf prisma/migrations

# 새 PostgreSQL 마이그레이션 생성
npx prisma migrate dev --name init

# 또는 개발 초기 단계라면 db push 사용
npx prisma db push
```

---

### Phase 7: Seed 데이터 검토

**파일:** `prisma/seed.ts`

SQLite와 PostgreSQL의 문법 차이 확인:
- `upsert`, `createMany` 등 Prisma API는 동일하게 동작
- 단, seed에서 raw SQL 쿼리 사용 시 문법 차이 확인 필요

```bash
# seed 실행
npx prisma db seed
```

---

## 배포 환경별 PostgreSQL 옵션

| 서비스 | 특징 | 무료 티어 |
|--------|------|-----------|
| **Supabase** | PostgreSQL 완전 관리형, 한국 리전(ap-northeast-2) | 500MB, 2개 프로젝트 |
| **Neon** | 서버리스 PostgreSQL, 자동 일시정지 | 0.5 CU, 3GB |
| **Railway** | 간단한 배포, 자동 백업 | $5/월 크레딧 |
| **Vercel Postgres** | Vercel 배포와 통합 | 256MB |
| **로컬 Docker** | 개발용 | 무료 |

### 권장: Supabase (이미 프로젝트에서 사용 가능성 높음)

---

## 체크리스트

### Phase 1 — 패키지
- [ ] `@prisma/adapter-better-sqlite3` 제거
- [ ] `better-sqlite3` 제거
- [ ] `@types/better-sqlite3` 제거

### Phase 2 — 스키마
- [ ] `prisma/schema.prisma` provider를 `postgresql`로 변경
- [ ] datasource에 `url = env("DATABASE_URL")` 추가
- [ ] (선택) 서버리스 환경이면 `directUrl = env("DIRECT_URL")` 추가

### Phase 3 — db.ts
- [ ] SQLite 어댑터 import 제거
- [ ] `path` import 제거
- [ ] `createPrismaClient` 함수를 표준 PrismaClient로 교체

### Phase 4 — 환경변수
- [ ] `.env` DATABASE_URL을 PostgreSQL 연결 문자열로 변경
- [ ] `.env.example` 업데이트
- [ ] 프로덕션 환경변수 설정 (Vercel/배포 서비스)

### Phase 5 — 마이그레이션
- [ ] PostgreSQL DB 인스턴스 준비
- [ ] 기존 SQLite 마이그레이션 히스토리 정리
- [ ] `prisma migrate dev --name init` 실행
- [ ] 마이그레이션 성공 확인

### Phase 6 — 검증
- [ ] `npx prisma studio`로 테이블 생성 확인
- [ ] Seed 데이터 정상 삽입 확인
- [ ] 앱 로컬 실행 후 로그인/회원가입 동작 확인
- [ ] AI Worker CRUD 동작 확인
- [ ] Stripe 결제 플로우 동작 확인

### Phase 7 — 정리
- [ ] `prisma/dev.db` 파일 삭제
- [ ] `prisma/prisma/dev.db` 파일 삭제
- [ ] 루트 `dev.db` 파일 삭제
- [ ] `.gitignore`에 `*.db` 항목 확인

---

## 주의사항

1. **기존 데이터 마이그레이션**: 운영 데이터가 있다면 SQLite → PostgreSQL 데이터 이전 스크립트 별도 작성 필요
2. **Prisma 7.x 패턴**: 이 프로젝트는 Prisma 7.x를 사용하므로 `prisma.config.ts`가 schema 설정을 오버라이드함
3. **Connection Pooling**: Vercel 등 서버리스 환경에서는 PgBouncer를 통한 연결 풀링 필수
4. **타입 차이**: PostgreSQL은 SQLite보다 타입이 엄격함 (BIGINT, UUID, JSON 등)
5. **트랜잭션 격리**: PostgreSQL의 기본 격리 수준은 `READ COMMITTED` (SQLite는 `SERIALIZABLE`)
