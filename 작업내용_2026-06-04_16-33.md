# 작업내용 — 2026-06-04 16:33

## 1. "검토 중" 상태 분석 및 문서화

- `reviewing` 상태가 DB·레이블·통계 카드는 있지만 전환 UI/API가 없어 항상 0임을 분석
- `검토중_상태_체크.md` 저장
- **결론**: 방안 A(버튼 추가 완성) 또는 방안 B(제거) 선택 필요

---

## 2. Hydration 오류 수정

**파일**: `src/components/worker/PurchaseButton.tsx`

- **원인**: `price.toLocaleString()` — 로케일 미지정으로 Node.js 서버와 브라우저 출력 불일치
- **수정**: `formatPrice(price)` (`Intl.NumberFormat("ko-KR")`)로 교체 → 서버/클라이언트 동일 결과 보장

---

## 3. AI 실행 에러 메시지 개선

**파일**: `src/components/worker/WorkerExecutor.tsx`

### 변경 전
원본 에러 문자열 그대로 노출:
```
401 {"type":"error","error":{"type":"authentication_error","message":"invalid x-api-key"},...}
```

### 변경 후
- `parseExecuteError()` 함수 추가 — 알려진 에러 패턴을 한국어 요약으로 변환
- UI: `AlertCircle` 아이콘 + **요약 메시지** + `오류 상세 보기` 접기/펼치기 버튼
- 상세 클릭 시 원문 전체 노출 (`<pre>` 블록)

### 인식 패턴

| 원문 키워드 | 표시 메시지 |
|---|---|
| `invalid x-api-key`, `authentication_error` | AI API 키가 유효하지 않습니다. |
| `insufficient_quota`, `billing` | API 크레딧이 부족합니다. |
| `rate_limit`, `too many requests` | API 요청 한도를 초과했습니다. |
| `context_length`, `too long` | 입력 내용이 너무 깁니다. |
| `model_not_found` | 선택한 AI 모델을 찾을 수 없습니다. |
| `timeout` | AI 응답 시간이 초과되었습니다. |
| 그 외 | AI 실행 중 오류가 발생했습니다. |

---

## 4. 관리자 패널 — 검수대기 링크 추가

**파일**: `src/app/admin/page.tsx`

- 검수 대기 / 게시 가능 / 반려된 섹션 워커 제목 → `<Link href="/market/[id]">` 적용
- 검수대기 카드에 "상세 보기" 버튼 (`ExternalLink` 아이콘) 추가
- Stats 상단 "검수 대기" 숫자 카드 → `#review-queue` 앵커 링크로 변경 (노란색 테마)
- 검수 대기 섹션에 `id="review-queue"` 추가

---

## 5. market/[id] 관리자 접근 허용

**파일**: `src/app/market/[id]/page.tsx`

- 관리자는 모든 상태 워커 조회 가능 (`where: { id }` — 상태 제한 없음)
- 미게시 워커 접근 시 "관리자 미리보기 · 현재 상태: OOO" 배너 표시

---

## 6. 관리자 AI 직원 실행 권한

**파일**: `src/app/api/execute/route.ts`

- `isAdmin` 조건 추가 → 관리자는 상태·구매 여부 관계없이 모든 워커 실행 가능

---

## 수정된 파일 목록

```
src/components/worker/PurchaseButton.tsx
src/components/worker/WorkerExecutor.tsx
src/app/admin/page.tsx
src/app/market/[id]/page.tsx
src/app/api/execute/route.ts
검토중_상태_체크.md              (신규)
작업일지_2026-06-04.md          (신규)
작업내용_2026-06-04_16-33.md    (신규)
```
