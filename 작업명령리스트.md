# 작업 명령 리스트

## 1. 로컬 Git 상태 확인

```powershell
git status --short
git branch --show-current
git remote -v
git log --oneline --decorate -5
```

용도:
- 현재 브랜치 확인
- 원격 저장소 연결 확인
- 최신 커밋이 `origin/main`과 일치하는지 확인

## 2. 로컬 프로덕션 빌드 확인

```powershell
npm run build
```

용도:
- 배포 실패 원인이 코드 빌드 오류인지 먼저 분리

## 3. 현재 Vercel 배포 상태 확인

```powershell
npx vercel inspect ai-worker-platform.vercel.app
```

용도:
- 현재 production alias가 어떤 배포를 가리키는지 확인

## 4. 최근 배포 목록 확인

```powershell
npx vercel ls ai-worker-platform
```

용도:
- 최근 production 배포 시간순 확인
- 반복 재배포 여부 확인

## 5. 특정 배포의 빌드 로그 확인

```powershell
npx vercel inspect ai-worker-platform-hkueydxr2-pandol69kim-5684s-projects.vercel.app --logs
```

용도:
- 실제 어떤 커밋으로 빌드됐는지 확인

확인 포인트:

```text
Cloning github.com/pandol69kim-bot/ai-worker-platform (Branch: main, Commit: ...)
```

## 6. 배포 메타데이터를 JSON으로 확인

```powershell
npx vercel list ai-worker-platform --format=json
```

용도:
- 각 배포의 `githubCommitSha`, `githubCommitMessage`, `action` 확인

중요 포인트:
- `action: redeploy` 이면 옛 배포를 다시 올린 것
- 최신 push 자동배포인지 수동 재배포인지 구분 가능

## 7. 특정 커밋으로 생성된 배포 찾기

```powershell
npx vercel list ai-worker-platform -m githubCommitSha=3538b6f5f330c376d47993332c29c1a0ba777a0a --format=json
```

용도:
- 원하는 최신 커밋으로 생성된 배포 URL 찾기

## 8. 최신 정상 배포 상세 확인

```powershell
npx vercel inspect ai-worker-platform-qtk48h4ft-pandol69kim-5684s-projects.vercel.app
```

용도:
- 최신 커밋 기반 배포가 `Ready` 상태인지 확인

## 9. 최신 정상 배포를 production으로 승격

```powershell
npx vercel promote ai-worker-platform-qtk48h4ft-pandol69kim-5684s-projects.vercel.app --yes
```

용도:
- 예전 커밋으로 덮인 production alias를 최신 정상 배포로 복구

## 10. 승격 후 production alias 재확인

```powershell
npx vercel inspect ai-worker-platform.vercel.app --format=json | Select-String 'githubCommitMessage|githubCommitSha|url|name|target'
```

용도:
- production alias가 실제로 원하는 배포 URL로 바뀌었는지 확인

## 이번 이슈의 원인

- 최신 커밋 `3538b6f` 자동배포는 정상 생성됨
- 그 뒤 예전 커밋 `b964cdb` 배포를 여러 번 `redeploy`
- 그 결과 production alias가 다시 옛 커밋으로 덮임

## 운영 원칙

- 옛 배포에서 `Redeploy`를 누르면 그 시점의 옛 커밋이 다시 배포됨
- 최신 코드를 운영에 반영하려면 아래 둘 중 하나를 사용

```powershell
# 방법 1: 최신 배포 승격
npx vercel promote <latest-deployment-url> --yes

# 방법 2: 새 커밋 push 후 자동배포 사용
git push origin main
```