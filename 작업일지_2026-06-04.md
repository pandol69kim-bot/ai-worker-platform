# 작업일지 — 2026-06-04

## 1. Import 오류 수정

### `getAIModelLabel` / `getAIProviderLabel`
- **파일**: `src/components/worker/PublishedWorkersManager.tsx`
- **원인**: `@/components/ui/select`에서 import → 실제 위치는 `@/lib/ai/catalog`
- **수정**: import 경로 수정

### `getAIModelOptions` / `getDefaultModelForProvider`
- **파일**: `src/components/worker/WorkerForm.tsx`
- **원인**: 존재하지 않는 함수명 사용
- **수정**: `getAIModelsForProvider`, `DEFAULT_AI_MODEL_BY_PROVIDER`로 교체

---

## 2. 관리자 패널 — AI API 키 관리 추가

### DB 스키마
- `AIProviderKey` 모델 추가 (`prisma/schema.prisma`)
- `prisma db push`로 Supabase 반영

### 신규 파일
| 파일 | 내용 |
|---|---|
| `src/lib/ai/keys.ts` | DB 키 우선 조회 → ENV fallback, 키 마스킹 유틸 |
| `src/app/api/admin/ai-keys/route.ts` | GET / POST(upsert) / DELETE |
| `src/components/worker/AdminAiKeyManager.tsx` | 키 등록·변경·삭제 UI |

### Provider 업데이트
- `openai.ts` / `gemini.ts` / `claude.ts` — `process.env` 직접 참조 → `getProviderApiKey()` 호출로 변경

---

## 3. 관리자 패널 — 회원 관리 추가

### 신규 파일
| 파일 | 내용 |
|---|---|
| `src/app/api/admin/users/route.ts` | GET(목록·검색·페이지네이션) / PATCH(역할 변경) |
| `src/components/worker/AdminUserManager.tsx` | 회원 테이블 + AI 직원 필터링 UI |

### 기능
- 이름·이메일 검색 (debounce 400ms)
- **역할 필터** (전체 / 일반 사용자 / 메이커 / 관리자)
- 역할 필터 선택 시 하단에 해당 역할 회원의 AI 직원 목록 표시
  - AI 직원 상태 필터 (전체 / 초안 / 검수대기 / 게시됨 등)
- 드롭다운으로 역할 즉시 변경
- 페이지네이션 (20명씩)

### Stats 카드
- 전체 회원 수 카드 추가 (`#users` 앵커 링크)

---

## 4. 다크모드 UI 수정

### `src/app/globals.css`
- `border-gray-100` 다크 오버라이드 추가 (`#1e293b`)
- `divide-gray-100 / divide-gray-200` 다크 오버라이드 추가 (Tailwind v3·v4 셀렉터 호환)
- `.admin-table tbody > tr + tr` 테이블 행 구분선 직접 타겟팅

### `src/components/worker/AdminUserManager.tsx`
- `divide-gray-100` → `divide-gray-200`, `border-gray-100` → `border-gray-200`로 통일
- 두 테이블에 `admin-table` 클래스 추가

---

## 5. AI 직원 생성 오류 수정

### Prisma 클라이언트 캐시 문제
- **증상**: `Unknown argument 'aiProvider'` — 구버전 클라이언트가 캐시됨
- **수정**: `.next` 캐시 삭제 + `prisma generate` 재실행

### JSON.parse 오류
- **파일**: `src/components/worker/WorkerForm.tsx`
- **원인**: `res.json()` 무조건 호출 → 서버가 HTML 오류 페이지 반환 시 파싱 실패
- **수정**: `res.text()` → `JSON.parse()` try-catch → 실패 시 응답 내용 직접 표시

### API 에러 메시지 개선
- **파일**: `src/app/api/workers/route.ts`, `src/app/api/workers/[id]/route.ts`
- catch 블록에 `console.error` 추가, 실제 에러 메시지를 응답에 포함
- `PrismaClientValidationError` 별도 분기 처리
- 중첩 try-catch → 단일 try-catch로 구조 정리

---

## 6. 관리자 패널 — 링크 및 접근 권한 개선

### 검수대기 카드 링크
- 검수 대기 / 게시 가능 / 반려된 섹션 워커 제목 → `<Link href="/market/[id]">` 적용
- 검수 대기 카드에 "상세 보기" 버튼 (`ExternalLink` 아이콘) 추가
- Stats 상단 "검수 대기" 숫자 카드 → `#review-queue` 앵커 링크 추가

### market/[id] 관리자 접근 허용
- **파일**: `src/app/market/[id]/page.tsx`
- 관리자는 상태 제한 없이 모든 워커 조회 가능
- 미게시 워커 접근 시 "관리자 미리보기 · 현재 상태: OOO" 배너 표시

### 관리자 실행 권한
- **파일**: `src/app/api/execute/route.ts`
- 관리자는 상태·구매 여부 관계없이 모든 워커 실행 가능

### workers API makerRole 필터
- **파일**: `src/app/api/admin/workers/route.ts`
- `makerRole` 쿼리 파라미터 추가 → 해당 역할 회원의 AI 직원만 필터링

---

## 수정된 주요 파일 목록

```
prisma/schema.prisma
src/app/admin/page.tsx
src/app/market/[id]/page.tsx
src/app/api/execute/route.ts
src/app/api/workers/route.ts
src/app/api/workers/[id]/route.ts
src/app/api/admin/ai-keys/route.ts      (신규)
src/app/api/admin/users/route.ts        (신규)
src/app/api/admin/workers/route.ts
src/components/worker/AdminAiKeyManager.tsx   (신규)
src/components/worker/AdminUserManager.tsx    (신규)
src/components/worker/PublishedWorkersManager.tsx
src/components/worker/WorkerForm.tsx
src/lib/ai/keys.ts                      (신규)
src/lib/ai/execute.ts
src/lib/ai/providers/openai.ts
src/lib/ai/providers/gemini.ts
src/lib/ai/providers/claude.ts
src/app/globals.css
```
