# AI 직원 메이커 플랫폼 프로그램 설계도

## 1. 프로젝트 개요

- 목적: 전문가의 업무 노하우를 "AI 직원" 상품으로 제작, 검수, 유통, 판매, 실행할 수 있는 마켓플랫폼
- 사용자 역할:
  - 일반 사용자: AI 직원 탐색, 구매, 실행, 리뷰 작성
  - 메이커: AI 직원 제작, 수정, 검수 요청, 판매 수익 확인
  - 관리자: AI 직원 검수, 승인/반려/게시 처리
- 기술 스택:
  - 프론트엔드: Next.js 16 App Router, React 19, TypeScript
  - 인증: NextAuth v5 beta, Credentials 기반 로그인
  - DB: Prisma + SQLite + better-sqlite3 adapter
  - 결제: Stripe
  - AI 실행: OpenAI API

## 2. 상위 아키텍처

```text
[Browser]
   |
   v
[Next.js App Router]
   |- 페이지 렌더링 (src/app)
   |- API Route (src/app/api)
   |
   +--> [Auth Layer: src/lib/auth.ts]
   +--> [DB Layer: src/lib/db.ts -> Prisma]
   +--> [AI Layer: src/lib/openai.ts -> OpenAI]
   +--> [Payment Layer: src/lib/stripe.ts -> Stripe]
   |
   v
[SQLite Database via Prisma]
```

## 3. 디렉터리 책임 분리

### 3.1 App Router 영역

- `src/app/page.tsx`
  - 메인 랜딩 페이지
  - 게시된 AI 직원 수, 누적 도입 수 등 플랫폼 통계 표시
- `src/app/(auth)/login`, `src/app/(auth)/register`
  - 로그인/회원가입 화면
- `src/app/market`
  - AI 직원 목록/상세 탐색
- `src/app/studio`
  - 메이커의 AI 직원 제작 및 수정 영역
- `src/app/dashboard`
  - 내 AI 직원, 구매 내역, 수익/판매 요약
- `src/app/admin`
  - 관리자 검수 및 승인 영역

### 3.2 API 영역

- `src/app/api/auth/*`
  - 로그인 세션 처리 및 회원가입 API
- `src/app/api/workers/*`
  - AI 직원 목록 조회, 생성, 상세 조회, 제출 처리
- `src/app/api/admin/workers/*`
  - 관리자 검수 액션 처리
- `src/app/api/purchases/*`
  - 구매 요청, 구매 내역 조회
- `src/app/api/payments/webhook/*`
  - Stripe 웹훅 연동
- `src/app/api/execute/*`
  - AI 직원 실행 및 실행 이력 저장
- `src/app/api/reviews/*`
  - 리뷰 작성/조회

### 3.3 공통 컴포넌트

- `src/components/layout/Navbar.tsx`
  - 세션 상태에 따라 메뉴 분기
  - 관리자 권한이면 관리자 메뉴 노출
- `src/components/providers/SessionProvider.tsx`
  - 클라이언트 전역 세션 공급
- `src/components/worker/*`
  - AI 직원 생성, 실행, 구매, 관리자 액션 관련 UI
- `src/components/ui/*`
  - 버튼, 카드, 입력 컴포넌트 등 UI 기본 요소

### 3.4 인프라/도메인 유틸

- `src/lib/auth.ts`
  - Credentials 로그인 처리
  - JWT/Session 콜백에서 사용자 ID, role 주입
- `src/lib/db.ts`
  - Prisma Client 싱글턴 관리
  - SQLite 절대 경로 계산
- `src/lib/openai.ts`
  - AI 직원 실행용 시스템 프롬프트 조합
  - OpenAI Chat Completions 호출
- `src/lib/stripe.ts`
  - 결제 Intent 생성

## 4. 핵심 도메인 모델

### 4.1 User

- 공통 사용자 엔터티
- `role` 값으로 `user`, `maker`, `admin` 역할 구분
- AI 직원 제작, 구매, 리뷰, 정산과 연결

### 4.2 MakerProfile

- 메이커 상세 정보
- 소개, 전문 분야, 수익 정보 저장

### 4.3 AIWorker

- 플랫폼의 핵심 상품
- 주요 속성:
  - 제목, 설명, 카테고리
  - 역할 정의, 워크플로우, 프롬프트, 규칙, 출력 형식
  - 가격, 가격 타입
  - 상태(`draft`, `approved`, `rejected`, `published` 등)
  - 판매/평점 집계 지표

### 4.4 AdminReview

- 관리자 검수 기록
- 승인/반려/게시 이력을 남김

### 4.5 Purchase

- AI 직원 도입 기록
- 무료 도입과 유료 결제 모두 표현
- Stripe 결제 ID 및 상태 관리

### 4.6 Review

- 사용자 리뷰 및 평점
- 사용자-직원 조합 유니크 제약 존재

### 4.7 Execution

- AI 실행 이력
- 입력, 출력, 상태, 에러, 토큰, 실행 시간 기록

### 4.8 Payout

- 메이커 정산 정보
- 현재 구조상 정산 관리 확장 지점

## 5. 주요 사용자 흐름

### 5.1 회원가입/로그인 흐름

1. 사용자가 회원가입 화면에서 계정 생성
2. 비밀번호는 해시 처리 후 DB 저장
3. 로그인 시 Credentials Provider가 이메일/비밀번호 검증
4. 세션에 사용자 ID와 role이 포함됨
5. Navbar와 각 보호 페이지가 세션/권한에 따라 분기 렌더링

### 5.2 메이커 제작 흐름

1. 로그인 사용자가 제작 센터로 이동
2. AI 직원 생성 폼 입력
3. `/api/workers` POST로 초안 생성
4. 이후 수정 및 검수 요청
5. 관리자 승인 또는 반려 후 게시 가능 상태 전환

### 5.3 관리자 검수 흐름

1. 관리자가 관리자 페이지에서 검수 대상 확인
2. `/api/admin/workers/[id]/review` 호출
3. 액션에 따라 상태 변경:
   - approve -> 승인
   - reject -> 반려
   - publish -> 게시
4. 검수 기록은 `AdminReview`에 저장
5. 게시 시 `publishedAt` 기록

### 5.4 구매 흐름

1. 사용자가 마켓 상세에서 도입 버튼 클릭
2. 무료 상품이면 즉시 `Purchase` 생성 후 완료 처리
3. 유료 상품이면 Stripe Payment Intent 생성
4. 결제 완료 후 웹훅 또는 후속 처리로 `paymentStatus = completed`
5. 구매 완료 시 AI 직원 실행 권한 획득

### 5.5 AI 실행 흐름

1. 사용자가 입력값과 `workerId`를 전달
2. `/api/execute`에서 권한 검사:
   - 무료 직원은 바로 실행
   - 유료 직원은 구매 여부 확인
   - 메이커 본인은 자신의 직원 실행 가능
3. `Execution` 레코드를 `running`으로 생성
4. OpenAI 호출 후 결과 저장
5. 성공 시 `completed`, 실패 시 `failed`로 상태 업데이트

## 6. 화면 설계 개요

### 6.1 메인 페이지

- 플랫폼 가치 제안
- 카테고리 탐색 진입
- 메이커/구매자 사용 흐름 소개
- 플랫폼 특징 및 CTA 제공

### 6.2 로그인/회원가입 페이지

- 이메일/비밀번호 기반 인증
- 로그인 성공 시 메인으로 이동

### 6.3 마켓

- 카테고리/검색/정렬/페이지네이션 기반 목록 조회
- 게시된 AI 직원 중심 노출
- 관리자일 경우 상태 필터 확장 가능

### 6.4 대시보드

- 내 AI 직원 현황
- 판매량 및 수익 요약
- 구매한 AI 직원 목록

### 6.5 제작 센터

- AI 직원 생성/수정
- 프롬프트와 워크플로우 관리
- 검수 제출 진입점

### 6.6 관리자 페이지

- 검수 대상 목록
- 승인/반려/게시 처리

## 7. API 설계 원칙

- App Router의 Route Handler 사용
- 입력값 검증은 `zod` 기반
- 인증이 필요한 API는 `auth()`로 세션 검사
- 에러 응답은 JSON 메시지 기반
- 권한 분기는 라우트 내부에서 role 확인

## 8. 인증/권한 설계

### 8.1 인증 방식

- NextAuth Credentials Provider 사용
- 이메일/비밀번호 해시 검증
- 세션 전략은 JWT

### 8.2 권한 분리

- 비로그인 사용자
  - 공개 페이지 접근 가능
  - 구매/실행/생성 등 보호 기능 제한
- 로그인 사용자
  - 구매, 리뷰, 일부 실행 가능
- 메이커
  - AI 직원 생성/관리
- 관리자
  - 검수 및 게시 권한

## 9. 데이터 저장소 설계

- DB 엔진: SQLite
- ORM: Prisma
- 개발 편의상 로컬 파일 DB 사용
- Prisma Client는 개발 환경에서 글로벌 싱글턴 재사용

## 10. 외부 연동 설계

### 10.1 OpenAI

- AI 직원 실행 엔진
- 입력 조합 방식:
  - 역할 정의
  - 업무 플로우
  - 규칙
  - 사용자 입력
- 현재 기본 모델: `gpt-4o-mini`

### 10.2 Stripe

- 유료 AI 직원 결제 처리
- Payment Intent 생성 중심 구조
- 웹훅을 통한 결제 완료 상태 반영 확장 가능

## 11. 시드 데이터

- 관리자 계정: `admin@aiworker.dev`
- 메이커 계정: `maker@aiworker.dev`
- 샘플 AI 직원 1건 포함
- 개발 환경에서 기능 검증용 초기 데이터 역할

## 12. 현재 설계상의 장점

- Next.js App Router 기반으로 화면/API가 한 저장소에 정리됨
- Prisma 스키마가 도메인 모델을 비교적 명확하게 표현
- 인증, 결제, AI 실행 계층이 `src/lib`로 분리됨
- 관리자 검수 흐름이 별도 API와 모델로 분리되어 있음

## 13. 보완 필요 포인트

### 13.1 역할 모델 정교화

- `role`이 문자열 상수라 오타나 상태 확장에 취약
- enum 수준으로 더 엄격하게 관리하는 것이 바람직

### 13.2 상태 머신 명확화

- `AIWorker.status`의 전체 전이 규칙을 문서화하거나 enum으로 고정할 필요가 있음

### 13.3 정산 로직 구체화

- `Payout` 모델은 존재하지만 자동 집계/정산 배치 로직은 아직 설계 확장이 필요

### 13.4 결제 완료 후속 처리

- Stripe 웹훅과 `Purchase` 상태 동기화 규칙을 명확히 정의해야 함

### 13.5 검색/추천 고도화

- 현재 목록 검색은 기본 `contains` 수준
- 향후 태그, 평점, 판매량 기반 추천 고도화 가능

### 13.6 보안/운영성

- 환경변수 누락 검증
- 관리자 API 감사 로그
- 요청 단위 rate limit
- 실행 비용 통제 정책이 추가되면 운영 안정성이 올라감

## 14. 향후 확장 방향

1. AI 직원 버전 관리 및 배포 이력 기능 추가
2. 구독형 상품의 만료/갱신 로직 구체화
3. 메이커 수익 대시보드 고도화
4. 관리자 검수 체크리스트와 피드백 템플릿 추가
5. 실행 결과 저장/재실행/공유 기능 추가
6. 팀/기업 단위 워크스페이스 개념 확장

## 15. 한 줄 요약

이 프로젝트는 "전문가 지식을 AI 직원 상품으로 만들어 판매하고, 사용자가 구매 후 즉시 실행할 수 있게 하는" 3면 구조의 AI 마켓플랫폼이며, 현재 구현은 인증, 제작, 검수, 구매, 실행의 기본 사이클을 모두 포함하는 형태로 설계되어 있다.