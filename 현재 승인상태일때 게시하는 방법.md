# 현재 승인상태일때 게시하는 방법

## 개요

이 프로젝트에서 `승인됨(approved)` 상태는 검수를 통과한 상태이지만, 아직 마켓에 공개된 상태는 아닙니다.

실제 마켓에 노출되려면 최종 상태가 `게시됨(published)` 이어야 합니다.

즉,

- `approved` = 검수 통과
- `published` = 실제 게시 완료

## 현재 동작 방식

관리자 액션은 다음 순서로 분리되어 있습니다.

1. 메이커가 검수 요청
2. 관리자가 `승인`
3. 상태가 `approved` 로 변경
4. 관리자가 다시 `게시`
5. 상태가 `published` 로 변경

## 어디서 확인하나

### 1. 상태 라벨 정의

상태 라벨은 아래 파일에 정의되어 있습니다.

- [src/lib/utils.ts](c:/Users/codelab/Desktop/ai-worker-platform/src/lib/utils.ts)

여기서 주요 상태는 다음과 같습니다.

- `draft`: 초안
- `submitted`: 검수 요청
- `reviewing`: 검토 중
- `approved`: 승인됨
- `rejected`: 반려
- `published`: 게시됨

### 2. 관리자 버튼 노출 조건

관리자 버튼은 아래 파일에서 결정됩니다.

- [src/components/worker/AdminWorkerActions.tsx](c:/Users/codelab/Desktop/ai-worker-platform/src/components/worker/AdminWorkerActions.tsx)

현재 로직은 다음과 같습니다.

1. `currentStatus === "submitted"` 이면 `승인` 버튼 표시
2. `currentStatus === "approved"` 이면 `게시` 버튼 표시

즉, 이미 승인된 AI 직원은 관리자 화면에서 `게시` 버튼을 다시 눌러야 공개됩니다.

### 3. 실제 상태 변경 API

상태 변경은 아래 API에서 처리됩니다.

- [src/app/api/admin/workers/[id]/review/route.ts](c:/Users/codelab/Desktop/ai-worker-platform/src/app/api/admin/workers/%5Bid%5D/review/route.ts)

액션별 상태 변경은 다음과 같습니다.

- `approve` -> `approved`
- `reject` -> `rejected`
- `publish` -> `published`

또한 `publish` 일 때만 `publishedAt` 이 기록됩니다.

## 현재 승인 상태일 때 게시하는 실제 방법

### 방법 1. 관리자 화면에서 게시 버튼 누르기

가장 정상적인 방법입니다.

1. 관리자 계정으로 로그인
2. 관리자 페이지 진입
3. 해당 AI 직원이 `approved` 상태인지 확인
4. `게시` 버튼 클릭
5. 상태가 `published` 로 변경되면 마켓 공개 완료

## 체크 포인트

### 메이커가 보는 제작 센터

- `승인됨` 은 게시 완료가 아닙니다.
- 현재는 승인된 상태에서도 미리보기/실행은 가능하지만, 일반 사용자 마켓 공개는 아닙니다.

### 구매 API

- 구매는 `published` 상태인 AI 직원만 허용됩니다.
- 따라서 `approved` 상태에서는 일반 사용자가 구매할 수 없습니다.

관련 파일:

- [src/app/api/purchases/route.ts](c:/Users/codelab/Desktop/ai-worker-platform/src/app/api/purchases/route.ts)

### 마켓 목록 노출

- 마켓 목록은 `published` 상태만 조회합니다.

관련 파일:

- [src/app/api/workers/route.ts](c:/Users/codelab/Desktop/ai-worker-platform/src/app/api/workers/route.ts)
- [src/app/market/page.tsx](c:/Users/codelab/Desktop/ai-worker-platform/src/app/market/page.tsx)

## 한 줄 정리

현재 승인 상태일 때 게시하려면, 관리자 계정으로 들어가 해당 AI 직원에 대해 `게시` 액션을 한 번 더 수행해야 합니다.