# 현재 프로젝트 배포방법

## 1. 현재 배포 기준 요약

2026-06-02 기준 현재 프로젝트의 실제 배포 전제는 아래와 같습니다.

- Next.js 16 App Router
- Prisma + PostgreSQL
- `@prisma/adapter-pg`
- Supabase PostgreSQL 사용
- NextAuth 인증
- Stripe 결제/웹훅
- OpenAI API 사용

즉, 예전 SQLite 기준 문서가 아니라 **Supabase PostgreSQL 기준 배포**로 보는 것이 맞습니다.

## 2. 현재 권장 배포 방식

현재 구조에서는 두 가지가 가능합니다.

### 2.1 권장 1순위: Vercel + Supabase

현재 DB가 파일 기반이 아니라 Supabase PostgreSQL이므로, 이제는 Vercel 배포가 현실적인 선택입니다.

적합한 이유:

- Next.js App Router와 Vercel 궁합이 좋음
- DB가 외부형 PostgreSQL이라 서버리스 환경과 충돌하지 않음
- Stripe webhook도 공개 도메인 기준으로 연결 가능

### 2.2 대안: VPS + PM2 + Nginx

Vercel이 아니라 직접 서버 운영을 원하면 Linux VPS 배포도 가능합니다.

이 경우에도 DB는 서버 로컬 파일이 아니라 **Supabase PostgreSQL**을 그대로 사용하면 됩니다.

## 3. 배포 전에 먼저 확인할 것

로컬에서 아래가 정상이어야 합니다.

```bash
npm install
npx prisma generate
npm run lint
npm run build
```

추가 확인:

- 로그인/회원가입 정상
- AI 직원 생성 정상
- 관리자 승인/게시 정상
- 리뷰 작성 정상
- Stripe 결제 흐름 정상
- Stripe webhook 수신 정상
- OpenAI 실행 정상

## 4. 현재 필수 환경변수

현재 프로젝트 기준으로 필요한 핵심 환경변수는 아래입니다.

```env
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres"
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

NEXTAUTH_SECRET="32바이트이상랜덤문자열"
NEXTAUTH_URL="https://your-domain.com"

NEXT_PUBLIC_APP_URL="https://your-domain.com"

OPENAI_API_KEY="sk-..."

STRIPE_SECRET_KEY="sk_live_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

PLATFORM_FEE_PERCENT=20
```

설명:

- `DATABASE_URL`: 앱 런타임 연결용
- `DIRECT_URL`: Prisma migrate용 direct 연결
- `NEXTAUTH_URL`: 실제 서비스 도메인
- `NEXT_PUBLIC_APP_URL`: 프론트 리턴 URL 기준

## 5. 반드시 수정해서 맞춰야 하는 부분

현재 코드와 설정 파일 사이에 한 가지 불일치가 있습니다.

현재 클라이언트 코드는 아래 변수를 읽습니다.

- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`

하지만 현재 `.env.example`과 `vercel.json`은 아래 이름으로 적혀 있습니다.

- `STRIPE_PUBLISHABLE_KEY`

즉, 현재 배포 시에는 반드시 아래처럼 맞춰야 합니다.

- 실제 배포 환경변수에는 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` 사용
- 가능하면 `.env.example`과 `vercel.json`도 같은 이름으로 정리

## 6. Vercel 배포 절차

### 6.1 Vercel 프로젝트 연결

1. Git 저장소를 Vercel에 연결
2. Framework Preset은 Next.js 선택
3. Root Directory는 저장소 루트 사용

현재 `vercel.json`이 존재하므로 기본 빌드 동작은 아래 기준입니다.

```json
{
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs"
}
```

### 6.2 Vercel 환경변수 설정

Vercel Dashboard에서 아래 값을 등록합니다.

- `DATABASE_URL`
- `DIRECT_URL`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`
- `NEXT_PUBLIC_APP_URL`
- `OPENAI_API_KEY`
- `STRIPE_SECRET_KEY`
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `PLATFORM_FEE_PERCENT`

중요:

- 현재 `vercel.json`에는 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`가 아니라 `STRIPE_PUBLISHABLE_KEY`가 들어 있습니다.
- Vercel Dashboard에는 **반드시 `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`를 직접 등록**해야 합니다.

### 6.3 Prisma 관련 주의

현재 `prisma.config.ts`는 아래 기준입니다.

- schema: `prisma/schema.prisma`
- migrations path: `prisma/migrations`
- datasource: `DATABASE_URL`, `DIRECT_URL`

하지만 현재 작업 상태상 확인된 점:

- `prisma/migrations` 폴더가 아직 루트에 없을 수 있음
- 예전 SQLite 흔적이 `prisma/prisma/*` 아래 남아 있을 수 있음

따라서 배포 전에 아래를 반드시 확인해야 합니다.

```bash
npx prisma migrate status
```

만약 루트 migration이 아직 없다면 개발 환경에서 먼저 정리합니다.

```bash
npx prisma migrate dev --name init
```

운영 배포에서는 아래를 사용합니다.

```bash
npx prisma migrate deploy
```

### 6.4 첫 배포 전 체크 순서

권장 순서:

1. `npx prisma generate`
2. `npx prisma migrate status`
3. 필요 시 `npx prisma migrate dev --name init`
4. `npm run build`
5. Vercel 배포
6. 배포 후 Stripe webhook URL 등록

## 7. Stripe 운영 배포 체크

Webhook endpoint 예시:

```text
https://your-domain.com/api/payments/webhook
```

필수 확인:

- `STRIPE_SECRET_KEY`
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `NEXT_PUBLIC_APP_URL`
- `NEXTAUTH_URL`

권장 이벤트:

- `payment_intent.succeeded`

배포 후 확인:

- 결제 성공 후 `Purchase.paymentStatus = completed`
- `AIWorker.totalSales` 증가
- `MakerProfile.totalEarned` 반영

## 8. NextAuth 배포 체크

현재 프로젝트는 세션 기반 인증을 사용하므로 배포 후 아래를 확인해야 합니다.

- `NEXTAUTH_SECRET` 설정 여부
- `NEXTAUTH_URL`이 실제 서비스 도메인과 일치하는지
- 배포 전 로컬 로그인 세션이 남아 있으면 재로그인 필요

최근 확인된 이슈:

- DB가 바뀐 뒤 예전 세션의 `user.id`가 남아 있으면 외래키 오류가 날 수 있음
- 이런 경우 로그아웃 후 재로그인하면 해결됨

## 9. VPS 배포 절차

Vercel 대신 VPS를 쓸 경우에도 DB는 Supabase를 사용합니다.

### 9.1 서버 준비

```bash
sudo apt update
sudo apt install -y nginx
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g pm2
```

### 9.2 프로젝트 배치

```bash
cd /var/www
mkdir -p ai-worker-platform
cd ai-worker-platform
git clone <저장소주소> .
```

### 9.3 환경변수 작성

`.env`에 4번의 값을 입력합니다.

### 9.4 설치 및 실행

```bash
npm ci
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 start npm --name ai-worker-platform -- start
pm2 save
```

### 9.5 Nginx 연결

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

## 10. 현재 배포 전 필수 체크리스트

- [x] DB provider가 PostgreSQL인지 확인
- [x] `src/lib/db.ts`가 `PrismaPg`인지 확인
- [x] Supabase `DATABASE_URL` 형식 확인
- [x] Supabase `DIRECT_URL` 형식 확인
- [x] `npx prisma generate` 성공 확인
- [ ] `npx prisma migrate status` 최종 확인
- [ ] 루트 `prisma/migrations` 정리 여부 확인
- [ ] `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` 기준으로 환경변수 정리
- [ ] `npm run build` 최종 확인
- [ ] 배포 후 로그인/생성/결제/웹훅 QA

## 11. 현재 기준 가장 현실적인 배포 결론

현재 프로젝트는 이미 SQLite 단계를 지나 **Supabase PostgreSQL 기반으로 배포 가능한 상태**입니다.

따라서 현재 기준 1순위 배포안은 아래입니다.

1. Supabase DB 유지
2. Prisma migration 상태 정리
3. Stripe 공개키 환경변수 이름 정리
4. Vercel에 배포

직접 서버 운영이 필요하면 VPS도 가능하지만, 현재 구조에서는 **Vercel + Supabase 조합이 가장 자연스럽습니다.**